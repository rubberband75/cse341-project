<!DOCTYPE html>
<html lang="en">

<head>
    <% include ./partials/head.ejs %>
</head>

<body>
    <% include ./partials/nav.ejs %>

        <div class="container">
            <br>
            <div class="section">
                <div class="row">
                    <div class="col s12 m5">
                        <img class="responsive-img" src="<%= product.imageUrl %>">

                        <% if (isAuthenticated) { %>
                            <form action="/project/01/cart/" method="POST">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <input type="hidden" name="productId" value="<%= product._id %>">

                                <button class="btn btn-large waves-effect waves-light cyan" type="submit" name="action"
                                    style="width: 100%;">Add To Cart
                                    <i class="material-icons right">add_shopping_cartt</i>
                                </button>
                                </a>
                            </form>
                            <% } %>
                    </div>
                    <div class="col s12 m7">
                        <div class="section">
                            <h5>Title</h5>
                            <p>
                                <%= product.title %>
                            </p>
                        </div>
                        <div class="divider"></div>
                        <div class="section">
                            <h5>Description</h5>
                            <p>
                                <%= product.description %>
                            </p>
                        </div>
                        <div class="divider"></div>
                        <div class="section">
                            <h5>Price</h5>
                            <p>$<%= product.price %>
                            </p>
                        </div>
                    </div>
                </div>

            </div>

        </div>

        <% include ./partials/footer.ejs %>
            <script>
            </script>
</body>

</html>